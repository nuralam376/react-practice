import React from 'react';

function Logo() {
    return (
        <div
            // className={`${styles.logo} ${styles.fill}`}
            // className={[styles.logo, styles.fill].join(' ')}
            style={{ color: 'green' }}
        >
            <h1>Logo</h1>
        </div>
    );
}

export default Logo;
